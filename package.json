{
  "name": "blocker-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"convex dev\" \"vite\"",
    "dev:frontend": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler pages deploy dist --project-name=blocker",
    "test": "vitest",
    "test:run": "vitest run",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "convex": "^1.17.0",
    "@convex-dev/auth": "^0.0.74",
    "@auth/core": "^0.37.0"
  },
  "devDependencies": {
    "@vitest/ui": "^2.0.0",
    "concurrently": "^9.0.0",
    "jsdom": "^25.0.0",
    "vite": "^5.0.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.24.0"
  }
}
